// We can use constants to create arrays.

const name = ["Morsh", "Dean", "Manny", "Dorothy"];

// name = ["Tina", "Henry"] T
// his will create error as constant value can't be modified.

name.push("Shivam", "Rajeev");
console.log(name);

// Here we can see that though constants can't be modified, constant arrays can include new elements inside them.
